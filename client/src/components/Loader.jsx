const Loader = () => (
  <div className="loader" role="status" aria-live="polite">
    <span className="spinner" aria-hidden="true" />
    <span>Searching pincode details...</span>
  </div>
);

export default Loader;
